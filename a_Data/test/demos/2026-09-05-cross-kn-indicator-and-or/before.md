# 原本实现

## 当时长什么样

- 策略买条件、卖条件各是一棵树。同一层里「收盘下穿布林」可以写。
- 但 K0 最低价下穿布林下轨 **并且** K1 最低价下穿布林下轨，会提示：一边按这一根 K 取值，另一边按更高层虚拟K取样，编不过。
- 收盘、RSI、布林跨层都不能用 AND 或 OR 拼。一条比较里把 K0 数字拿去穿 K1 布林，本来就不该过。

## 当时怎么操作

1. 股票 002003，默认区间，连续单步把布林走出来。
2. 打开策略回测，买条件加两行：K0 最低价下穿 K0 下轨，K1 最低价下穿 K1 下轨，中间选 AND。
3. 会编不过，跑不了。
