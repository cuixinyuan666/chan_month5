---
description: 全智能体长期记忆——接任务必读、确认执行门禁、task 演示、白话文案
alwaysApply: true
---

# 智能体长期记忆（常驻）

**接任务第一步**：读 [`AGENT_LONG_TERM_MEMORY.md`](../../AGENT_LONG_TERM_MEMORY.md) §0 + [`AGENTS.md`](../../AGENTS.md)。

## 确认执行门禁（最高优先级）

- 用户消息**无「确认执行」** → **禁止**改 app 关键逻辑（Rust `chan_data`、Flutter 步进/冻结/管道/主图语义等）。
- 需改关键逻辑 → 只用文字写修改方案，末尾写：**请回复「确认执行」后我再改代码。**

## 必做：Task Log + 演示

1. 完成后追加 [`task-log.md`](../../task-log.md)。
2. 演示 `before/after`/`walkthroughSteps` 用**白话**（K0、步进、副图、买卖点），禁止大段代码/函数名。

## 开发演示阶段（默认开）

冷启动自动加载最新任务演示；用户关「开发演示阶段」后不再自动加载。

## 禁止

- 未「确认执行」就改关键逻辑。
- 只提交代码不写 `task-log.md`。
- 删除 `lib/history/` 常驻按钮。
- 演示文案堆代码引用。
- 用「一键跳末」代替连续单步验收。
