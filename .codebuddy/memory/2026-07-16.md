# 2026-07-16

## CHAN_RUST chan_kline 渲染线型偏好（用户明确要求）
- 十字光标线（crosshair）统一实线：kline_chart.dart `_drawCrosshair` 内两处 `_drawDashedLine` → `canvas.drawLine`。
- 已冻结高层级连线（forLevel 的 frozenDashPattern）统一实线：移除 chart_level_line_style.dart 中 case 4/6/default 的 frozenDashPattern。
- 保留：构建中（building）连线仍用虚线，作"未确认 vs 已确认"区分。
- 口径：实线=已确认/辅助；虚线=仅未确认的构建中连线。

## 新增：构建中合并框（虚线）— 实线/虚线框呈现落地
- 口径（纠正）：构建中合并 = 每层 `combineFrames` **末组**（CombineEngine 仍可 absorb 的末组），虚线描边；前组实线=已冻结。**不是** `activeUnit`（那是进行中段/连线单元）。
- 实现：`_drawCombineFramesOnMainChart(lastAsBuilding:true)`；K0/K1/Kn 入口全层同构；`_strokeDashedRect` 虚线矩形。
- as-of：十字线用当步重建的 combineFrames，末组仍虚线（当下性由 as-of 重建保证，不再靠隐藏）。
- 历史记录：`appendBuildingCombineFrame()` 已同步纠正口径文案。
- 设计约束：全层同构（K0/K1/…/KN），已写入 AGENTS.md(CHAN_RUST段) 与 CHAN_RUST/README.md。

## 排障：flutter run 白屏卡死（构建中合并框虚线）
- 根因：加载后 `_stepIdx=0` 仅 1 根 K，`viewXMin==viewXMax` → `xSpan` 被夹成 `1e-6` → `slotW` 天文数字；末组合并框改虚线后 `_drawPatternLine` 沿超长边循环上亿次，UI 线程卡死（Responding=False），表现为白屏。
- 修复：`kline_viewport.dart` 的 `xSpan` 下限改为 `1.0`，单根时 `resetForBarCount` 视窗跨度=1；`_drawPatternLine` 增加 `isFinite`/步长守卫/迭代上限作兜底。
- 触发条件：勾选 K0合并（默认开）+ 首屏仅 1 根可见 K + 虚线描边。

## 新增：「构建中合并框」设置面板开关（默认开）
- 现状：该功能此前已实现（用 `_drawCombineFramesOnMainChart(lastAsBuilding:true)`，K0/K1/Kn 三层末组虚线，非 activeUnit 方案）。本次仅加开关门控。
- 改动：kline_chart.dart 给 `KlineChart` widget 与 `_KlineCompositePainter` 加 `showBuildingCombine`(默认 true)；三处 `lastAsBuilding:true` → `lastAsBuilding:showBuildingCombine`；build() 传 `widget.showBuildingCombine`。
- main.dart：加 `_showBuildingCombine=true` 状态、`SwitchListTile`（紧随「截断机制」开关同格式）与说明弹窗 `_showBuildingCombineHelp`（遵守增设置加弹窗约定）；KlineChart 构造传 `showBuildingCombine`；拨动 append 历史记录。
- 默认开；关=所有合并框实线。lint 全绿。

## 排障：用户误回退未提交文件（对比 HEAD 恢复）
- 现象：`git status` 仅显示本轮 4 个正常未提交改动；但用户反映可能误回退。回退是"文件被还原到 HEAD"，故被回退的未提交改动在 git 中显示为「干净」、看不出异常，需逐个核对关键文件内容。
- 实际被回退（未提交、被还原到 HEAD）的文件：
  1. `AGENTS.md`：缺失本轮第9步加的「增删改查约束」（HEAD 无此条）；已重新加到 CHAN_RUST 段，与 README 口径一致。
  2. `CHAN_RUST/flutter/chan_kline/lib/widgets/chart_level_line_style.dart`：case 6（K5连线）仍带 `frozenDashPattern:[12,4,2,4]`（虚线），违背"已冻结高层级连线统一实线"；已移除该赋值（case 4/default 此前已无，十字光标实线在 kline_chart.dart:2657 已确认保留）。
- 已确认完好的：README 增删改查约束、main.dart 开关、kline_chart.dart showBuildingCombine 门控与 crosshair 实线、构建中合并框特性（均已在 3085b0a/cf2527f/8d2742e 提交或本轮正常未提交）。
- 教训：本轮对 AGENTS.md / chart_level_line_style.dart 的关键改动未提交，易被编辑器/手动回退成 HEAD 而静默丢失；建议这些改动尽快 commit。

## 排障：flutter run 报 showBuildingCombine 未定义（第三处回退）
- 现象：`flutter run -d windows` 报 kline_chart.dart:699 No named parameter 'showBuildingCombine'；1470/2150/2267 getter 未定义。
- 原因：`_KlineCompositePainter` 的字段声明(~923)与构造参数(~887)被回退丢失，但 widget 侧(69/98/100)、build() 传参(699)、三处 `lastAsBuilding:showBuildingCombine` 仍在 → 编译失败。
- 修复：painter 补回 `this.showBuildingCombine = true`(构造)与 `final bool showBuildingCombine`(字段)；updateShouldRepaint(2856) 加 `oldDelegate.showBuildingCombine != showBuildingCombine`。lint 全绿。
- 补充教训：这类跨 widget/painter 的成对改动若只提交一半，回退后会一半在一半丢，必须整体核对。

## 排障：设置里看不到「构建中合并框」开关（第四处回退）
- 现象：用户反映最新 app 里看不到构建中合并框开关/虚线视觉语义。
- 原因：main.dart 里开关的 `SwitchListTile` UI 与向 `KlineChart` 传 `showBuildingCombine: _showBuildingCombine` 两处被回退丢失，只剩状态变量(_showBuildingCombine=124)与说明弹窗(_showBuildingCombineHelp=816)。因未传参，painter 用默认 true，虚线其实仍在画，但用户没有开关可见。
- 修复：main.dart 补回 KlineChart 构造传参 `showBuildingCombine: _showBuildingCombine`(510后)；截断机制开关(692)之后补回「构建中合并框」SwitchListTile（同格式+说明按钮+append 历史）。lint 全绿。
- 至此 showBuildingCombine 特性四处回退全部恢复：painter字段/构造(kline_chart)、build传参+updateShouldRepaint(kline_chart)、main开关UI+传参(main)。

## 扩展：「构建中合并框」开关 → 统一「构建中/未确认虚线」开关
- 需求：该按钮不再只管合并框，要统一控制所有「构建中/未确认」的画线（如 K0/K1/KN 构建中连线）。
- 改名：showBuildingCombine → showBuildingDash（main.dart 状态变量/帮助方法/传参、kline_chart.dart widget+painter 字段与构造与 3 处 lastAsBuilding 与 updateShouldRepaint 全部同步 rename）。
- 接入：
  - _drawBuildingLevelLine(K1/KN 构建中连线) 的 building:true → building:showBuildingDash（开关关则实线）；
  - _drawBuildingK0Line(K0 构建中连线) 原强制 _drawDashedLine → 受 showBuildingDash 控（关则 canvas.drawLine 实线）。此前 K0 构建线根本没接开关、且用独立样式，现与 K1/KN 同口径。
- UI/帮助：设置面板标题「构建中合并框」→「构建中/未确认虚线」，帮助弹窗与 subtitle、append 历史文案同步扩展为合并框+各层构建中连线。
- 语义：开=所有构建中/未确认元素虚线（区分未确认 vs 已确认）；关=全部实线（不区分）。全层同构 K0/K1/…/KN。lint 全绿。
- 注：本次改动尚未 commit（用户本轮未要求提交）。
