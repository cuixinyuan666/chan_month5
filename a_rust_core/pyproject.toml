[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "a-rust-core"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "a_rust_core_ext"
features = ["pyo3/extension-module"]
