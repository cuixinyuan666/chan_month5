# 工程术语教科书（从零开始的完整体系）

> **目标**：让初学者能够从零理解本工程的完整知识体系——术语的由来、各部分的设计意图、如何正确使用以及需要注意的细节。阅读完本文档后，您应能够在实际操作中精准定位问题，避免常见误区。

---

## 前言：为什么需要这套术语体系

在本工程中，**「图上看到的数」**（图表展示的指标值）与**「策略条件实际参与运算的数」**（真正用来判断买卖点的采样值）可能来自不同的时间采样频率。如果不加以区分，容易产生以下困惑：

- 明明在 K0 第 377 根看到 MACD 出现金叉，但策略却没有触发买入信号；
- 同一指标在不同图表层级（K0、K1、K2）上的数值含义不同，直接比较会导致误判；
- 条件触发的时间点与图表十字线显示的数值不一致，难以进行回溯验证。

为此，本工程提出了一套**统一的采样钟（Clock）体系**，通过明确区分**计算钟（evalClock）**、**铺时钟（plotClock）**、**成交钟（executionClock）**，并配套**冻结仓、可用性（availableAt）、当下性**等机制，保证：

1. 条件求值仅在真正可用的样本点上进行；
2. 图表展示是对冻结仓数据的可视化铺展，不参与运算；
3. 所有变量具有唯一的身份证（Variable ID），支持跨层级、跨指标的安全对比；
4. 步进式单步验证与冻结不回写的设计保证历史数据不可篡改，便于回测与实盘对齐。

以下章节将从零开始构建这套体系的每一块基石，并说明它们是如何协同工作的。

---

## 第一部分：基础概念——K 线层次与颗粒度

### 1.1 K0：原生 K 线

- **定义**：交易所直接产生的一根根 K 线，包含开盘、最高、最低、收盘、成交量（OHLCV）。
- **特征**：每根 K0 对应真实市场在一个固定周期内的价格波动，是所有后续运算的最细粒度基准。
- **在代码中的标识**：`displayKn == 0`、变量前缀 `RAW.K0.*`（例如 `RAW.K0.CLOSE`）。
- **教学举例**：打开任何交易软件，看到的红绿柱子就是 K0；每一根都是不可再分的基本单位。

### 1.2 虚拟 K 线（Kn，n ≥ 1）

- **定义**：按照缠论规则（合并、分型、段、中枢等）把多根 K0 **合成** 出来的更粗粒度的 K 线。只在关键转折点出现，因此样本是**稀疏**的。
- **生成逻辑**：
  1. 从 K0 序列中提炼出分型、笔、段；
  2. 按照段的方向和重叠情况构造中枢；
  3. 以中枢的起点、结束点以及关键节点为支撑，合成出虚拟 K 线的高、低、开、收；
  4. 每根虚拟 K 对应一个时间区间（由其对应的 K0 首尾根号决定）。
- **特征**：
  - 颗粒度随 `n` 增大而变粗（K1 比 K0 粗，K2 比 K1 更粗……）。
  - 只有在满足结构条件时才会产生新的虚拟 K，因而样本间隔不均匀。
- **代码中的标识**：`displayKn >= 1`、结构层 `level == kn-1`；虚拟 K 的 OHLC 保存在 `KnOhlcSample` 结构中。
- **教学举例**：假设一段上涨过程中只有三次明显的回调形成三个分型，则这三个分点连成的线就是第一层虚拟 K（K1）；再把更大级别的转折点连起来得到 K2，以此类推。

### 1.3 虚拟 K 的右端点（endX）

- **定义**：一根虚拟 K 对应的那根 K0 序号——即该虚拟 K “走完” 时，真实 K0 已经推进到的位置。
- **作用**：决定该虚拟 K 的指标值何时变为**可用**（availableAt），亦是冻结仓写入的时机。
- **举例**：若某根 K1 覆盖 K0 从第 5 根到第 12 根，则它的 endX 为 12；只有在 K0 第 12 根刚收完时，K1 的指标才能被冻结仓记录为可用。
- **代码字段**：`KnOhlcSample.endX`、对应的 UI 层 `UnitBar.x2`。

### 1.4 颗粒度（求值颗粒度）

- **定义**：在评估一个买卖条件时，**在哪根线上**进行真假判断——是逐根 K0 仍是只在虚拟 K 的右端点采样。
- **由计算钟（evalClock）决定**：
  - `k0Bar`：在每根 K0 上求值（密集采样）;
  - `knSample`：只在每根虚拟 K 的 endX 处求值（稀疏采样）。
- **沟通要点**：当说 “K1 MACD 在 K0@377 未被检查” 时，其实是在指出**计算钟是 knSample**，而 K0@377 不是虚拟 K 的采样点，因而条件在此根不参与运算。

---

## 第二部分：三套「钟」（计算钟、铺时钟、成交钟）

本工程的核心创新在于把“时间维度”显式建模为三种互不相干的钟，分别负责**运算**、**展示**、**执行**。

### 2.1 计算钟 / 求值钟（evalClock）

- **目的**：确定条件表达式在何时、在哪些样本点上进行真假运算。
- **两种取值**：
  | 取法       | 含义                         | 适用对象                     |
  |------------|------------------------------|------------------------------|
  | `k0Bar`    | 逐根 K0 逐根检查（密集）     | K0 原生指标（收盘、成交量、K0 布林等） |
  | `knSample` | 只在虚拟 K 右端检查（稀疏）  | Kn≥1 的指标（K1 布林、K1 MACD 等） |
- **代码实现**：`TradeEvalClock.k0Bar`、`TradeEvalClock.knSample`。
- **记忆技巧**：`k0Bar` 就像每根 K0 上都有一根“检测棒”，`knSample` 则只有在虚拟 K 出现时才插入一根棒。

### 2.2 铺时钟 / 显示钟（plotClock）

- **目的**：决定图表上**在哪些 K0 位置**把冻结仓里的数值“铺开”画出来，以供人眼观察。
- **目前仅有一种实现**：`TradePlotClock.k0Bar`——即所有指标（不管是 K0 还是 Kn）均按 K0 一根一格的方式均匀铺平，形成连续的曲线。
- **重要区分**：铺平后的数值**仅用于可视化**，**不参与**任何条件运算（否则会把稀疏的虚拟 K 样本误视为逐根 K0 样本，导致误触发）。
- **举例**：K1 布林中轨在图上看似每根 K0 都有一条线，但真正用来计算的只有虚拟 K 右端的少数几个点；图上的连续线是通过插值（最近邻或前值填充）得到的视觉效果。

### 2.3 成交钟（executionClock）

- **目的**：明确真实成交只能发生在 K0 时间轴上，避免出现“在虚拟 K 开盘价买入”这样的不可实现假设。
- **关键事实**：虚拟 K 是事后合成的，没有实际成交价；所有买卖只能在 K0 的开盘价或收盘价（取决于滑点模式）发生。
- **两种成交价取法**（都基于 K0）：
  | 模式                | 取价                         | 说明                                         |
  |---------------------|------------------------------|----------------------------------------------|
  | `sameBarClose`（默认） | 当根 K0 收盘价              | 信号产生后即以该根收盘价撮合（假设可以当日收盘成交） |
  | `nextBarOpen`       | 下一根 K0 开盘价             | 更保守；若没有下一根则订单失效                |
- **代码路径**：`trade_clock.dart` 中的 `TradeExecutionClock` 与 `TradeFillPriceMode`。

### 2.4 铁律：条件求值与展示铺平的分离

> **条件只能走变量自己的 `evalClock`；铺平后的 K0 格子是 `plotClock`，不是用来算 CROSS 的钟。**

- **为什么重要**：若直接使用铺平图表上的连续值来做穿越判断，实际上把稀疏样本当成了密集样本，会在虚拟 K 未出现的根上产生“假信号”。
- **正确做法**：
  1. 条件求值仅在变量所属的 `evalClock`（k0Bar 或 knSample）样本点上进行；
  2. 十字线、指标曲线等全部读取自冻结仓按 `plotClock`（k0Bar）铺平后的序列；
  3. 两者相互独立，只有在同一采样频率下（即两者均为 k0Bar）才可能数值一致。

---

## 第三部分：钟族（ClockFamily）——同层同钟才能比较

为进一步防止跨层级、跨指标的非法比较，本工程把所有指标划分到**钟族**，只有**同层、同钟族、同计算钟**的变量才允许进行直接比较或穿越运算。

### 3.1 zsMath 族（中枢数学族）

- **成员**：中枢、布林带、MACD、RSI、KDJ、DMI 等所有基于价格序列的统计/变换指标。
- **层级规则**：
  - K0：对应 `RAW.K0.*`（原生数据）；
  - Kn≥1：对应结构层 `level == kn-1` 的虚拟 K（即 `structure.kn.*`）。
- **举例**：K0 收盘价 (`RAW.K0.CLOSE`) 与 K1 布林中轨 (`SUB.K1.BOLL.MID`) 虽层不同，但均属 zsMath 族，可以在满足“同计算钟”前提下进行比较（如 `CROSS(RAW.K0.CLOSE, SUB.K1.BOLL.MID)`）。
- **代码标识**：`TradeClockFamily.zsMath`。

### 3.2 line 族（连线族）

- **成员**：比例线、斜率线、节奏线等所有依赖**结构点坐标**（如分型、笔段端点）的几何线段。
- **层级规则**：数据直接附着在结构层 `level == kn`（即虚拟 K 的端点）上。
- **举例**：K1 连线斜率 (`STRUCTURE.K1.LINE.SLOPE`) 与 K2 比例线 (`STRUCTURE.K2.LINE.RATIO`) 均属 line 族，可在同层同计算钟下比较。
- **代码标识**：`TradeClockFamily.line`。

### 3.3 混钟禁止原则

- **规则**：只有在 **同一结构层（layer）**、**同一钟族**、**同一计算钟 (evalClock)** 下，两个变量才能安全地进行等值比较、大小比较或穿越运算；否则编译期或运行时会被标记为非法。
- **典型非法案例**：
  - `RAW.K0.CLOSE` 与 `SUB.K1.BOLL.MID` 比较（不同层）；
  - `STRUCTURE.K1.LINE.SLOPE` 与 `SUB.K1.RSI` 比较（不同钟族：line vs zsMath）；
  - `RAW.K0.CLOSE` 与 `SUB.K1.MACD.HIST` 穿越（不同计算钟：k0Bar vs knSample）。
- **防护措施**：在 `signal_data_catalog.dart` 中为每个变量预先声明其 `layer`、`family`、`evalClock`；条件编译阶段会自动校验兼容性。

---

## 第四部分：冻结仓与铺平格——数据的生产、存储与可视化

### 4.1 冻结仓（FreezeStore）

- **定义**：每走一步（_stepIdx 前进一根 K0），所有已确认的指标值会被写入冻结仓对应的位置，**只写不重算**，历史格子永不回改。
- **核心属性**：
  - **步进当下性**：只有当前步及以前的格子才有值；未来格子为不可用 (`null`)。
  - **不可回写**：已冻结的数在后续步骤中不会被修改，确保可回溯。
- **实现类**：`MathSeriesFreezeStore`（对 MACD、布林、RSI、KDJ、均线、通道等指标）。
- **教学比喻**：想象一个只有向前写的仓库，每来一根新 K0，就把本步算好的指标放进对应的货架；之前的货架永远保持原样，不会被重新贴标。

### 4.2 铺平格（K0 格子）

- **定义**：冻结仓里按 K0 一根一根线性展开的数组；前端十字线、指标曲线等均从此处读取数值。
- **作用**：把可能是稀疏的虚拟 K 样本（**可用性**仅在特定 K0 上）通过“前值填充”或“最近邻”手段变成在每根 K0 上都有值的连续序列，以便绘图。
- **注意**：铺平格的值**仅用于显示**，不参与任何运算。

### 4.3 availableAt 与 discoveryX

- **availableAt**：某个指标值在冻结仓中第一次变为可用的 K0 索引（即该值对应的虚拟 K 的 endX）。
- **discoveryX**：与 availableAt 语义相同，但在成交价定位场景下使用（信号产生的 K0 位置）。
- **两者均基于 K0 轴**，是把虚拟 K 右端映射回真实时间基准的桥梁。
- **举例**：K1 布林上轨的某个样本在虚拟 K 第 3 根出现，对应的 K0 索引为 377，则该样本的 availableAt = 377；在 K0 第 377 根之前，该值是不可用的。

### 4.4 当下性与不可用（null）

- **当下性**：指每走一步，只会新增一格可用数据；历史格子锁死，不会因后续重计算而改变。
- **不可用**：数据尚未产生时一律为 `null`，**不填 0、不沿用前一根的值**；这样可以避免：
  - 将“尚未出现”误判为“数值为 0”;
  - 将“数值未变”误判为“数值继承”。
- **验证手段**：在回放或实盘中，若看到某指标在前几根为空白（null），则说明该层级尚未产生足够的样本进行计算——这是符合预期的，而不是程序错误。

---

## 第五部分：变量身份证（Variable ID）与信号产生机制

### 5.1 Variable ID（全局唯一标识）

- **格式**：`<panel>.K<layer>.<indicator>.<field>`  
  - `panel`：`RAW`（原始行情）、`SUB`（副图）、`MAIN`（主图）、`STRUCTURE`（结构点）等；
  - `layer`：`0` 表示 K0，`>=1` 表示虚拟 K 的层号；
  - `indicator`：指标名称，如 `CLOSE`、`MACD`、`BOLL`、`BUY1` 等；
  - `field`：指标内部字段，如 `HIST`、`MID`、`UPPER`、`LOWER`、`VALUE`。
- **举例**：
  - `RAW.K0.CLOSE`          → K0 原生收盘价；
  - `SUB.K1.MACD.HIST`      → K1 的 MACD 柱状图；
  - `MAIN.K1.BOLL.DOWN`     → K1 的布林下轨；
  - `STRUCTURE.K1.BUY1`     → K1 的一类买点（结构点）。
- **好处**：在条件编写、调试、日志中都能唯一定位一个数值，防止因命名冲突导致的逻辑错误。

### 5.2 前缀 CHAN. 与 STRUCTURE. 的等价

- **规定**：在条件表达式中，`CHAN.Kn.BUY1` 与 `STRUCTURE.Kn.BUY1` 完全等价，系统会在解析阶段自动归一化为后者。
- **目的**：让使用者在阅读缠论原文时更习惯用 `CHAN.` 前缀，而内部统一使用 `STRUCTURE.` 以便与其他面板区分。

### 5.3 等值比较（eq）的精度对齐

- **原因**：浮点数在二进制下无法精确表示某些十进制小数，直接比较 `==` 容易因微小误差而误判。
- **对齐规则**：
  - MACD 系列：保留 **3 位小数**；
  - RSI / KDJ 系列：保留 **2 位小数**；
  - 其余指标：依据其实际小数位数或在 `signal_data_catalog.dart` 中的 `displayDigits` 配置。
- **实现**：在 `condition_eval.dart` 中的 `TradeBinaryOp.eq` 会先把两端数分别四舍五入到对应位数，再进行整数比较。

### 5.4 边沿触发与脉冲事件

- **边沿触发（默认）**：条件从 **假 → 真** 的瞬间只产生一次信号；连续多根真只在第一根产生。
- **事件脉冲（Pulse Op）**：对于分型确认、一类买点等**瞬时事件**，规定**每根满足条件都产生一次信号**，即便连续多根也各自计数。
- **判定依据**：`condition_eval.dart` 中的 `_isPulseOp()` 函数（根据操作符类型决定是否为脉冲）。
- **举例**：  
  - 边沿：收盘 > 10 日均线，只有在当日刚好站上均线时才产生买信号；连续多日站上均线不再重复产生；  
  - 脉冲：K0 出现顶分型确认，则该根一定产生卖信号；如果第二天又出现顶分型确认，则第二天也产生卖信号。

### 5.5 穿越（CROSS）的特殊规则

- **基本规则**：在当根样本点上如果前后两根的大小关系发生改变（从小于等于到大于，或从大于等于到小于），则产生一次穿越信号。
- **抑制条件**：若此时已有**状态条件**（如 `A > 0`）使得整个表达式在左侧或右侧恒为真，则该穿越被视为冗余，**不再产生信号**（防止因“一直为真”导致的无效重复触发）。
- **实现位置**：`condition_eval.dart` 中关于 `prevFlag` 与 `_isPulseOp` 的综合判断逻辑（第 333–348 行）。

### 5.6 信号（SignalEvent）的结构

- **组成字段**：
  - `discoveryX` / `availableAt`：触发信号的 K0 索引；
  - `variableId`：产生信号的指标全名；
  - `direction`：买入还是卖出（`BUY` / `SELL`）；
  - `strength`：信号强度（可选，用于后续过滤）；
  - `timestamp`：对应的真实时间戳（若有）。
- **产生时机**：仅在变量所属的 `evalClock` 样本点上，当条件满足边沿触发或脉冲事件规则时才会实例化一次 `SignalEvent` 并写入 `msg_history.dart` 的会话历史。

---

## 第六部分：操作动词与验收语言（步进、冻结、跳末等）

| 术语          | 大白话解释                                                                 |
|---------------|----------------------------------------------------------------------------|
| **步进 / 单步** | `_stepIdx` 每前进一根 K0，冻结仓只追加新格，历史不改——体现“步进当下性”。   |
| **跳末**      | 一次性把 `_stepIdx` 拉到最后一根 K0（用于预热或快速回看），**不等于**逐步单步验收。 |
| **冻结不回写**  | 旧步的格子永不被修改，只在尾部追加新数据。                                 |
| **asOf / 走到 K n** | 当前已推进到的 K0 根号；条件只判断到此根为止，以后的根对本次计算“不可见”。  |
| **不可用**    | 数据尚未产生时为 `null`，不填 0、不继承前值。                             |

> **验收建议**：在调试时请使用**逐根单步**（步进）进行观察，确认每步冻结仓的新增值与预期一致；切勿仅用“一键跳末”来判断逻辑正确性，因为跳末会跳过中间状态的检查。

---

## 第七部分：采样门控与动态段（高级主题）

### 7.1 冻段与动态段（Virtual K 的内部结构）

- **冻段**：已经完全确认、后续不会再改变的虚拟 K 单元（unitBars）；对应的指标值已写入冻结仓且永不回改。
- **动态段**：当前正在形成的、尚未满足确认条件的那一段虚拟 K；其端点 (`endX`) 会随 `asOf`（当前已走到的 K0）而变化；此段上的指标值仍处于“待确认”状态，**不参与**条件求值（只在冻段上求值）。
- **意义**：正是因为有动态段的存在，才能保证“步进当下性”——只有当动态段被确认转为冻段时，其值才真正成为可用样本。

### 7.2 门控量（Volume）与门控笔数（Number）

- **定义**：在某一层级 (Kn) 只有当其 **确认门控**（即累计成交量或笔数达到一定阈值）后，才会产生对应的成交量、笔数序列；在此之前这些序列一律为 **不可用**。
- **作用防止**：避免在趋势尚未明确、能量不足时把虚假的成交量当作有效信号使用。
- **使用场景**：副图成交量指标、资金流指标等均需先判断 `availableAt != null` 再参与运算。

### 7.3 evalIndex 与 availableAt 的区别

| 概念        | 含义                                         | 取值范围                     |
|-------------|----------------------------------------------|------------------------------|
| `evalIndex` | 该变量在所属 **计算钟** 上的样本序号（从 0 开始） | 0,1,2,...（随冻段增长）      |
| `availableAt`| 该样本对应的 **K0 索引**（真实时间基准）      | 递增的整数（每次新样本对应的 endX） |
| **举例**    | 某 K1 MACD 第 2 个样本（evalIndex=1）对应的 K0 索引为 345，则 availableAt=345。                |

### 7.4 未来安全（futureSafe）

- **定义**：变量声明中的一个布尔字段，所有已登记的指标均为 `true`，意为：
  - 取值过程中**不会读取尚未到来的 K0**（即不存在 look-ahead bias）；
  - 历史值在 `asOf` 推进后**不会被回写**（冻结不回写）；
  - 因此所有基于该变量的条件在回放和实盘中具有一致的因果关系。
- **检查点**：在 `signal_data_catalog.dart` 中每个 `TradeVariableDef` 均需显式设定 `futureSafe: true`；若出现 `false` 则表示该变量依赖未来信息，需谨慎使用（通常仅用于演示或离线研究）。

---

## 第八部分：信号产生方式三大类（边沿 / 脉冲 / 穿越）

| 方式       | 触发条件                                 | 信号产生频率                               | 典型用例                      |
|------------|------------------------------------------|--------------------------------------------|------------------------------|
| **边沿触发** | 条件从 **假 → 真** 的瞬间                | 连续真只产生一次（首次为真时）             | 收盘 > 均线、RSI > 70        |
| **事件脉冲** | 条件本身是**瞬时事件**（如分型确认）     | 每根满足条件都产生一次，即使连续也各自计数  | 一类买点、顶/底分型确认      |
| **穿越 (CROSS)** | 两条线在当根样本点上前后关系发生改变   | 当根产生一次；若因状态条件已恒真则抑制     | 价格上穿布林中轨、MACD 金叉  |

> **实现细节**：所有三种方式的判定均集中在 `condition_eval.dart` 的 `evalCompiledCond` 函数中，通过 `_isPulseOp`、`prevFlag` 与状态抑制逻辑共同决定是否在当根 `emitSignal()`。

---

## 第九部分：买卖点类号体系（N 类统一）

为便于在条件积木中灵活选择买卖点层级，本工程把**三类及以上**的买卖点统一为 **N 类**，用类号（class）进行区分。

| 类号 | 变量名示例                 | 含义                         |
|------|---------------------------|------------------------------|
| 1    | `STRUCTURE.Kn.BUY1` / `SELL1` | 一类买点 / 一类卖点          |
| 2    | `STRUCTURE.Kn.BUY2` / `SELL2` | 二类买点 / 二类卖点          |
| 3+   | `STRUCTURE.Kn.BUY_N.3` / `SELL_N.3` | 三类买点 / 三类卖点（以此类推） |
| 0 或 -1 | `STRUCTURE.Kn.BUY_N.0` / `SELL_N.0` | 该方向**全部**类号（一类+二类+三类及以上） |
- **关键常量**（定义在 `_bbbuy_n_var.dart`）：
  - `kTradeMinBsClass = 3`：N 类起始类号（从三类开始）；
  - `kTradeUiMaxBsClass = 6`：条件积木 UI 中展示的最大类号；
  - `kTradeMaxBsClass = 20`：系统实际支持的最大类号；
  - `kTradeAllBsClass = 0`：代表“全类”的特殊值（落盘时统一写为 `BUY_N.0` 或 `SELL_N.0`）。
- **中文类名**：1→“一”、2→“二”、…、10→“十”；超过 10 直接显示阿拉伯数字（便于 międzynarod 环境）。
- **使用建议**：在编写条件时若想捕捉“任何级别的买点”，可直接使用 `STRUCTURE.Kn.BUY_N.0`；若只想要“一类或二类”，则需要显式写出两个变量的逻辑或（`BUY1 OR BUY2`）。

---

## 第十部分：最佳实践与常见误区

### 1. 永远记得检查 `availableAt`

> 在引用任何副图或结构指标前，先确认 `variableId.availableAt != null`（或使用内置的 `isAvailable(variableId)` 辅助函数），否则条件将在不可用样本上误判为假。

### 2. 区分“图上看到”和“条件计算”

- **图上看到** → 读取 `plotClock`（k0Bar）铺平后的冻结仓；
- **条件计算** → 读取所属 `evalClock`（k0Bar 或 knSample）的原始样本；
> 两者只在 **两者均为 k0Bar** 时数值才会一致；否则请不要用图上数值直接做运算。

### 3. 禁止混钟比较

> 在写条件时请使用 IDE 提供的变量提示（会标注层级、钟族、evalClock），若出现红色警告，立刻检查是否跨层或跨族。

### 4. 使用脉冲操作时注意连续触发

> 某些指标（如分型确认）本质是事件脉冲，若把它当成普通边沿条件使用，会在连续出现时漏掉后续信号；请确认对应操作符是否被标记为 `_isPulseOp`。

### 5. 回测与实盘保持一致的步进方式

> **验收标准**：在策略回测时，必须使用**逐根单步**方式推进 `_stepIdx`，并检查每步冻结仓新增值是否与实盘逐笔推送一致；不可用“一键跳末”代替单步验证。

### 6. 关于精度对齐的调试技巧

> 若怀疑因浮点误差导致信号迟早或缺失，可在条件中先把变量乘以 1000（MACD）或 100（RSI/KDJ），转为整数再比较，以消除小数位影响。

---

## 第十一部分：一页速查表（适用于现场调试）

```
K0            = 原生每根           → RAW.K0.* / 冻结仓逐格
Kn (n≥1)      = 虚拟 K 关键点      → 结构层 level==kn-1
计算钟 k0Bar  = 逐根 K0 检查（与图上格子同源）
计算钟 knSample= 虚拟 K 右端检查（稀疏）
plotClock k0Bar= 铺成 K0 一格一格，只看不算
成交钟        = 永远 K0（开盘/收盘价）
==            = MACD 三位小数 / RSI 两位小数对齐
边沿          = 假→真 只打一次
事件脉冲      = 当根各打一次，连着两颗各各打
冻结仓        = 存好不回写、不重算
当下性        = 步进一步存一格，历史不改
不可用        = null，不填 0，不沿用上一根
混钟禁忌      = 同层同钟族才能 CROSS / 比较
```

---

## 第十二部分：结语与后续学习路径

- **掌握本文档** 即可在日常策略编写、调试、回测中准确使用术语，避免因采样不一致导致的逻辑漏洞。
- **进阶建议**：
  1. 阅读 `CHAN_RUST/README.md` 中的 “实现约束与口径” 章节，了解底层 Rust 实现如何保证零拷贝、线程安全；
  2. 研究 `trade_clock.dart`、`signal_data_catalog.dart`、`condition_eval.dart` 三个核心文件，了解钟、变量、条件的具体绑定；
  3. 在 `msg_history.dart` 中追踪 `SignalEvent` 的写入与冻结仓的快照，体会“当下性”与“不可用”在历史回放中的表现；
  4. 尝试在条件积木中加入自定义指标，遵循 `signal_data_catalog.dart` 的注册模板，亲身验证钟族、evalClock、availableAt 的声明效果。

> 祝您使用愉快，若在实践中遇到任何术语歧义或行为异常，请回顾本章节对应内容，或在开发者群中提出具体的变量名和使用场景，我们将帮助您定位是**采样钟**、**冻结仓**还是**混钟**方面的误区。

--- 

*文档版本：v1.2（2026-09-15）  
基于分支：ANDROID_RUST  
适用范围：Flutter 端 + Rust 后端（chan_ffi.dll）  
维护人：量化AI编码机器人（Hermes Agent）*