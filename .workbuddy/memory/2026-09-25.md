# 2026-09-25 工作日志

## 解释性文档剪切迁移到 Obsidian（承接 09-24 会话，00:00 前后完成）
- 用户要求把 GLOSSARY.md 这类解释性文档「剪切」到 Obsidian。确认范围=index-docs 清单全部 13 篇、仓库全删不留占位、删后 commit+push。
- 关键顺序：**先把仓库最新内容覆盖进 vault，再删仓库**（vault 里 GLOSSARY 是 09-22 旧版 v1.2，仓库已是 v1.3 含背驰专题，直接删会丢 3KB 内容）。覆盖后校验「背驰专题」「v1.3」都在。
- 删除：GLOSSARY.md、CHAN_RUST/README.md、CHAN_RUST/docs 8 篇、docs/superpowers 3 篇 → commit e0408d22 + push，0/0。仓库根只剩 AGENTS/CLAUDE/OPENCODE/task-log；CHAN_RUST/docs 只剩 design_mockup.html。
- 死引用修掉两处：app_debug_snapshot.dart「详见仓库 GLOSSARY.md」→ 指向 Obsidian；AGENTS.md:50 技术口径 → docs/CHAN_RUST_README。vault 的 index-docs.md 口径改为「正本在 Obsidian」。
- 刻意不迁：AGENTS.md、CLAUDE/OPENCODE 指针、a_Data/test/demos（测试夹具）、design_mockup.html。
- **sync 脚本坑**：默认 --date 取「今天」，跨零点后再补同步前一天条目会被跳过（不是 bug）。补同步须显式加 --date 参数，例如 --date 2026-09-24。
- **老坑复现防范**：bash -c 传含反引号的 Python 字符串会被命令替换——本次全部改用临时 .py 脚本文件写入，未再污染。
