# 2026-07-14

## CHAN_RUST WSL 支持
- README 增加「本机支持 WSL」说明，新增「构建与运行（WSL / Linux）」章节及依赖提示（clang/cmake/ninja/gtk3）。
- 新建 `CHAN_RUST/scripts/build_rust.sh`：对应 build_rust.ps1，Linux 产物为 `libchan_ffi.so`，复制到 `flutter/chan_kline/linux/native/` 及 debug bundle `build/linux/x64/debug/bundle/lib/`。
- Dart 桥 `chan_bridge.dart` 已有 Linux 分支加载 `libchan_ffi.so`，无需改动。

## CHAN_RUST 主图指标命名统一（KN合并/KN连线）
- 用户要求按（K0合并,K0连线）、（K1合并,K1连线）规律统一命名规则，防后续误判。
- 统一规则：主图指标 `combine(n)` / `line(n)` 的 `n` 都=层号（与连线一致）。
  - K0合并=combine(1)、K1合并=combine(2)、K2合并=combine(3)…；连线 line(1/2/3)=K0/K1/K2连线。
  - 绘制分发：combine(1)→_drawKlineCombineOnMainChart(widget.combineFrames)；combine(2)→_drawBiCombineOnMainChart(widget.biCombineFrames)；combine(>=3)→_drawLevelCombineOnMainChart(kn)（从 levels[b.level==kn].combineFrames 取）。
- 顺带修复两个老 bug：① 目录循环原 `n<=maxCombine` 多生成无对应层的合并项（越界）；② 通用合并分支原 `b.level==kn` 应为层号（编号统一后即正确）。
- 改动文件：`flutter/chan_kline/lib/models/chart_indicator.dart`（kn 注释、label `K${kn-1}合并`、目录循环 combineMax）、`lib/widgets/kline_chart.dart`（hasK0Combine 用 combine(1)；分发 combine(1/2/>=3)）、`lib/main.dart`（默认选中 combine(1)）。
- 已确认主图指标无本地持久化（仅内存 Set），编号 0→1 不影响旧设置。`flutter analyze` 0 新增错误（仅 2 条 pre-existing deprecated 告警）。
- K0/K1 渲染逻辑原样保留只换编号；K2+合并此前缺失/错显，现已补全。

## CHAN_RUST 副图指标命名统一（分型确认/极点距/截断）
- 用户要求把副图三组指标（分型确认/极点距/截断）也按主图「统一层号」规律梳理：原副图用 `0..maxKn-1` 且 `level=kn+1`，与主图层号口径不一致；统一为 `kn=层号`(1..maxKn)，`level=kn`，展示名 `K${kn-1}xxx`（如 fractalConfirm(1)=K0分型确认，对应 level=1 的 confirms）。
- 改动文件：
  - `lib/models/chart_indicator.dart`：`buildSubIndicatorCatalog` 循环改 `n=1..maxKn`（原 `n=0..maxKn-1`/`maxFx`）；SubChartIndicator.kn 注释与 label `K${kn-1}分型确认/极点距/截断` 已对齐。
  - `lib/widgets/kline_chart.dart`：3 个副图绘制函数 `_drawKnFractalConfirmSubChart`/`_drawKnFractalPeakDistSubChart`/`_drawKnTruncationSubChart` 及 `_peakDistValueAt` 中 `level = labelKn + 1`→`level = labelKn`；回退 `labelKn==0`→`labelKn==1`；极点距颜色改为 bundle 存在时用 `forLevel(level)`，仅无 bundle 回退层用青色。
  - `lib/models/bar_feature_lookup.dart`（十字线 tooltip）：副图分支 `level=kn` 统一；`kn==0` 回退→`kn==1`（最低层回退旧 bi_confirm / barFeatures 极点距 / bi_confirm 截断）；`confirms[kn+1]`→`confirms[kn]`。
  - `lib/main.dart`：默认副图选中 `fractalConfirm(0)`→`fractalConfirm(1)`。
  - `lib/history/app_debug_snapshot.dart`：快照「命名与口径」段补述主图/副图统一层号规则。
  - `README.md`（CHAN_RUST 根）：命名历史段说明主图/副图统一层号、三组指标 kn 口径一致。
- 历史记录复制粘贴（msg_history）为自由文本，与 kn 编号无关，无需改动；复制/粘贴/快照按钮保留。
- 测试：`test/sub_indicator_peak_trunc_test.dart` 中原 `kn=0` 构造全部改为 `kn=1`（标签仍为 K0xxx，因 label=K${kn-1}）。`flutter analyze` 0 新增错误；`flutter test` 副图相关 3 项 + 其余共 16 项通过；仅 `widget_test.dart`(应用可挂载，查找 CHAN_RUST 标题) 环境性失败，与本次改动无关（标题硬编码 main.dart:58）。
- 未改动：helper `confirmMarkerShapeForKn`/`peakDistLineStyleForKn` 用 `labelKn % 5`/`% 4` 取模，层级号无关，无需改；主 tooltip `_levelBlockRows` 的「Kn 块显示 K(n+1) 端点确认」是另一套设计，不在本次范围内。
- CHAN_RUST 无 skills 目录，skills 梳理为 no-op。
