# 2026-07-15 工作日志

## CHAN_RUST 中枢(ZS)设计调研与方案
- 探索 `CHAN_RUST/rust/chan_data/src/` 确认现状：combine/fractal/line(on_confirm) 每层复用同构；**无 step() 增量接口**（README TODO）；**完全无 中枢/ZS 代码**（README TODO: 中枢 Rust 核心）。
- 关键可复用组件（真实路径）:
  - `LevelSegment`(pipeline.rs:76) 自带 high/low/begin_pole_x/end_pole_x → 直接当段区间，无需新 Range 类型。
  - `LevelBundleOut.segments`(pipeline.rs:194) 即中枢输入；`run_pipeline()->PipelineResult.levels`(pipeline.rs:236/962) 离线产物。
  - `MergedGroup::absorb`+`test_combine_range`(engine.rs:284/240) 是吸收器范本；中枢延伸照抄只换判定。
  - `FirstSegmentInput` trait(segment_first.rs:14) 给 LevelSegment 也 impl → find_zs 层无关。
  - `KlineCombineFrame`(combine.rs:22) 镜像产 ZSFrame 供 Flutter 复用合并框绘制。
  - `LevelConfirm`(pipeline.rs:52) 的 used/truncated/pole_x 供 classify_bsp 复用。
- 创新设计核心：**中枢 = CombineEngine 吸收器的「松重叠(≥3)孪生」**。同构吸收器级联：A(包含)→levels[n+1]；B(overlap≥3)→ZS_n（吃同一份 levels[n].segments）；C(≥2同向分离ZS)→走势类型。整套缠论结构是同一台 RangeAbsorber 级联堆叠。
- 落地约定：ZS.level 取 lv.level，显示名套 K${level-1} → 笔中枢=K0中枢/线段中枢=K1中枢，与 combine(1)=K0合并 对齐。无未来函数由「只吃已冻结 segments」自动继承；step() 补上后增量 feed 即可。
- 用户要求：仅设计、未落代码。下一步待用户拍板：① 写 zs.rs(ZS+find_zs+build_zs_for_levels) 离线接 run_pipeline 并对齐 Python ZS/；② 补 ZSFrame；③ classify_bsp。
