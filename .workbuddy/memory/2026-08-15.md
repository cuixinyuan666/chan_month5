# 2026-08-15 工作记录

## Tooltip 成交量/笔数循环补 asOf 截断（演示阶段交付完成）
- 在 `CHAN_RUST/flutter/chan_kline/lib/models/bar_feature_lookup.dart` 成交量(406)、笔数(448) 两个循环补 `if (asOf != null && bars[i].idx > asOf) continue;`，与 tooltip 其余区段当下性纪律对齐。
- 取值本身因果（`_accumulateConfirmGated` 累计到 i），无未来数据；本次为防御性，悬停显示不变。
- 配套交付：`a_Data/test/demos/2026-08-15-tooltip-vol-tick-asof/`（manifest.json + before.md + after.md，白话）。
- 验证：`flutter analyze` 0 error、本次改动零新增 issue；纯 Dart 改动，未碰 Rust，无需重编 DLL。
- 注：flutter SDK 实际路径为 `D:\a_flutter_SDK\flutter_windows_3.41.9-stable\flutter\bin\flutter.bat`（非记忆里的 `D:\flutter`），PowerShell 用原生 Windows 路径跑。

## 云端访问 chan_month5（trade 分支，无本地文件夹）
- 用户要求：不走本地克隆/文件夹，直接连云端仓库读 `trade` 分支，便于后续查看/分析/编辑。
- 仓库：`cuixinyuan666/chan_month5`（origin 即此；默认分支 `main`，目标操作分支 `trade`）。
- 连接方式：`gh` CLI（已登录 `cuixinyuan666`，带 `repo` 权限）走 GitHub REST API（经本机代理 `127.0.0.1:19828`）。不依赖本地工作区。
- `trade` HEAD：`53e965922495a301b2f9b85446911c410e3ea8af`（2026-08-15T07:50:54Z）。
- 查看文件：`gh api "repos/cuixinyuan666/chan_month5/contents/<path>?ref=trade" --jq '.content' | base64 -d`。
- 列目录：`.../contents?ref=trade`（根已列出 40+ 顶层条目）。
- 编辑（后续）：GET 取 `sha` → PUT `contents/<path>` 带 `{message,content(base64),sha,branch:"trade"}`；新建不带 sha；删除用 DELETE。全流程云端，无本地落地。
- 注：GitHub 连接器在 WorkBuddy 侧显示 disconnected，但 `gh` 认证可用，故用 gh CLI 直连云端。
