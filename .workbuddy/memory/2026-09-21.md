
## 2026-09-21 工具接入
- 安装 user-level skill `nox-grill-me`（grill me：对计划/设计持续追问直到达成共识）。
- 安装 user-level skill `obsidian-writer`（Obsidian 直接读写本地 vault，零依赖）。
- 发现本机 Obsidian vault：`C:\Users\86185\Documents\Obsidian Vault`，已固化进 obsidian-writer 默认路径与项目/用户记忆。
- 验证：写入 `00-inbox/obsidian-writer-test.md` 并读回成功，Obsidian 接入可用（零依赖直接读写文件）。
- 建立 Obsidian 项目文件夹 `chan-month5/`（Vault 顶层）：回填 `task-log.md` 的 **251 条 / 38 天**为 38 篇日期笔记 + `MOC.md` + 3 份索引（共 42 个 md），逐字保真，验收全过。
- 规则落地：`AGENTS.md`「完成后」新增 Obsidian 同步 4 步；项目 `MEMORY.md` 新增「Obsidian 任务过程记录约定」（`task-log.md` 为唯一源、Obsidian 为副本，禁止手改副本）。
- 闭环自验：按新规则把本次建库任务本身补记进 `task-log.md`（252 条）→ 同步 `chan-month5/2026-09-21.md`（4→5 条）→ 更新 `MOC.md` 时间线行与总数。校验：唯一日期 38 == 笔记数 38，无 CRLF，底栏导航完好。
- **K{n}回归通道落地（用户已「确认」）**：滑窗 N 收盘价最小二乘回归 → 中轨，上下轨 ±k×总体标准差（除 N，与布林同口径），默认 N=20/k=2.0。改动：`math_classic_compute.dart`（`RegressionChannelEngine`）、`chart_indicator.dart`（regressionChannel，归「均线」、层内序布林之后=13）、`kline_chart.dart`（中轨实线+上下轨虚线，`_paintPriceSeries` 加可选 dashPattern）、`math_series_freeze_store.dart`（regress(kn) 冻结仓）、`bar_feature_lookup.dart`（tooltip regress_mid/up/down）、`math_indicator_config.dart`（regressN/regressK）、`main.dart`（设置项+说明弹窗）、`msg_history.dart`（口径说明，标注未接回测/ML）。
- 验收：`flutter analyze` 无新增 error；`flutter test` 全量 345 过/10 跳/3 败，3 项失败（mini_loop BOLL CROSS、signal_data_catalog 布林冻结仓、widget_test 挂载）经 **HEAD 干净 worktree 基线复现**确认属改动前既有问题；phase2b3 为环境 OOM。回归通道单测 6 项全过（含「新增K不改写历史点」不变量）。
- 踩坑：本机跑 `flutter test` 会因 HTTP(S)_PROXY 走代理导致 `WebSocketException: Invalid WebSocket upgrade request`，**需在子进程 env 里剔除 proxy 变量并设 NO_PROXY=localhost,127.0.0.1** 才能跑通。
- 踩坑：task-log 出现并行重复条目（09-21 与 09-22 各一条回归通道），已去重保留 09-22 那条，MOC 计数同步修正为 253 条 / 39 天 / 39 篇。
