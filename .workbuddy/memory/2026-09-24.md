# 2026-09-24 工作日志

## 本地分支从云端更新（验证，no-op）
- 用户要求「本地分支从云端更新」，grill 后确认目标=当前分支 ANDROID_RUST，范围仅此一支。
- `git fetch origin` + `rev-list --left-right --count`：`ANDROID_RUST` 0 领先 / 0 落后；`git pull --ff-only` → "Already up to date." 工作树干净。结论：已与云端同一提交，无需更新。
- 其他分支落后云端：本地 `main` 落后 origin/main **174** 提交、`full-optimization` 落后 **9**；`CHAN_RUST`/`ar`/`clear` 均 0/0。用户明确不动 main / full-optimization。

## obsidian-task-recorder skill 缺陷修复
- 该 skill 的 SKILL.md 步骤 1 模板（`### 日期 · WorkBuddy · 类型 · 标题` + 操作/演示/测试/注意事项）与仓库 `task-log.md` 顶部「## 条目格式」权威模板（`### 日期 时间 — 标题` + 执行者/任务类型/上下文/关键操作/结果/注意事项）**不一致**。
- 已用 Edit 把 skill 模板改回与仓库一致；sync 脚本本身按 `### <date>` 头切分、与条目体格式无关，故本次同步正常（已验证 2026-09-24.md + MOC 更新到 286 条 / 41 天）。
- 教训：写/改任务记录时以仓库 `task-log.md` 的「## 条目格式」为唯一权威，不要照抄 skill 旧模板。

## 跨智能体 skills 同步（12:20）
- 新增用户级技能 `sync-agents-skills`（~/.workbuddy/skills/），`scripts/sync_agents_skills.py` 跨本机 5 个 SKILL.md 智能体（workbuddy/codebuddy/claude/cursor/opencode）双向 mesh 同步；跳过 trae/cline/continue 与 Cursor 专属 skill（update-cursor-settings/migrate-to-skills/update-cli-config）；支持 --dry-run/--check。已全量同步一次：WB↔其它 4 个互通，Cursor 22 通用 skill 拉回 WB。

## 设置 UI 清理：回归通道入口整合（12:34）
- 用户问「设置里的 K{n}回归通道 是否逻辑已不存在？不存在就删按钮」。核查：回归通道逻辑全在（计算/绘制/冻结仓 regressByKn/回测 MAIN.Kn.REGRESS.MID·UP·DOWN/十字读数），**不满足「不存在才删」**。用户明确预期=把该独立入口「集成到数学指标参数」。
- 改动（main.dart，非核心设置 UI，未触「确认执行」门禁）：删独立 `ListTile('K{n}回归通道')`（原 2719-2735）+ 过期副标题；把回归通道说明折叠进 `_showMathIndicatorHelp`「?」弹窗并更正「暂未接入回测/ML」为已接入；删孤立 `_showRegressionChannelHelp`；修 main.dart:165 过期注释（滑窗回归→父层连线绑定）。`dart format` 通过。
- 结论：回归通道功能保留完整，仅设置入口去重。改动**未提交**，按 auto-commit-obsidian 子任务流待单独 commit+push+record。

## 设置字符串全层级可复制（12:47）
- 用户要求「设置元素字符串可复制」强化到设置内所有子UI甚至子子UI。现状：设置面板 `_buildPanelBody` 已用 `SelectionArea` 包裹（main.dart ~2428），但由设置打开的 `showDialog`→`AlertDialog` 未包裹。
- 用括号配平脚本把 main.dart 12 个「设置簇」对话框（10 个说明弹窗 + 参数对话框 `_editMathIndicatorParams` + 确认对话框 `_askChipTickContinue`；`_promptTickQuality` 委托前者）的 `builder: (ctx) => AlertDialog(` 改为 `SelectionArea(child: AlertDialog(`，并自动补 `)` 配平。`dart format` 通过。
- 覆盖范围=面板→子对话框→子子UI 参数对话框，全部可鼠标拖选+Ctrl+C。改动**未提交**，待单独 commit+push+record。

## 设置4按钮合1：复制排查信息（12:58）
- 用户要求把「一键复制历史记录 / 查看历史记录 / 复制页面快照 / 复制调试信息」4 个按钮集成、去重、精简，合为 1 个。
- grill 确认：①单按钮弹「排查快照」对话框（查看+复制，替代“查看”+3复制）；②删 AppDebugSnapshot 里约60行静态“命名与口径”文档（改1行口径版本号）+ 其内嵌“历史最近10条”（历史改由整合器统一输出“最近20条”，消除与全量复制的两处重复）；③验收探针（audit_probe_snapshot）保留为段落，满足代码“常驻勿删”约束。
- 改动：`app_debug_snapshot.dart` 删静态文档+历史块+多余 import；`main.dart` 删 `_copyHistoryRecords`/`_copyDebugSnapshot`/`_copyAuditProbeDebug` 3 方法，新增 `_buildConsolidatedProbeText`+`_showConsolidatedProbeDialog`（含「完整历史」次级入口保留全量浏览/清空）；同步修正过期“复制调试信息”文案→“复制排查信息”。`dart format` 通过，改动**未提交**。

## 确认删除两演示按钮 + 清残留注释（13:03）
- 用户要求删设置里「手动打开最新任务演示」「任务演示列表/前后对比」两按钮。核查：两按钮在上一轮「删除开发演示阶段与任务演示子系统」已删（main.dart ~2553-2558、~2896-2939）；task_demo/ 目录与 _taskDemo/_devDemo 引用均不存在；settings 当前仅 刷新股票列表/复制排查信息/机器学习 等。用户确认「确认已删+清残留注释」。
- 仅清 msg_history.dart:412 过时 doc 注释「同页前后对比（常驻）」→「可演示验收（a_Data/test/demos 的 manifest + before/after 文件）（常驻）」；历史日志原文 1385 保留。main.dart:628「自动播放 N/M」为通用步进进度，非 task_demo，未动。`dart format` 通过，已记 Obsidian。改动**未提交**。
- 完成「K线筹码峰·纯量级」主图指标收尾：main.dart 删孤儿 `_showPeakRankModeHelp`；_editMathIndicatorParams 弹窗加「筹码峰数量 N」「峰线型」控件并落盘 `_chipConfig`。
- 校验修复 4 处：profile_peak_classify 纯分支 `row` 前向引用（上移）；catalog_lookup 调 `liveProfilePeakScalar(field:)` 但缺该参数（补回透传 peakScalarAt）；chip_peak_var_test 的 ingestClassified 缺 scheme（补 defaults.schemeId）；新增纯量级单测。
- 结果：flutter analyze 全包 0 error；profile_peak_classify_test 2/2、chip_peak_var_test 4/4 通过（清 proxy+no_proxy=*）。
- 注意：signal_data_catalog_test 中 BOLL 用例失败（MAIN.K0.BOLL.DOWN 期望2.0得null），走 MathSeriesFreezeStore 路径，与筹码峰无关，既有独立问题，未处理。改动未提交。
- 上述累积改动已综合提交并推送：commit e1654aea（feat: K线筹码峰·纯量级主图指标 + 删除演示子系统 + UI整合），push 至 origin/ANDROID_RUST，Obsidian 同步完成。
  工作树仅余环境生成文件（plugin_symlinks/local.properties/.flutter-plugins-dependencies/generated_config.cmake）未纳入版本控制。

## 新增主图指标「唐奇安通道」（23:10，grill 确认后执行）
- grill 四共识：经典口径（上轨=HHV(high,N)/下轨=LLV(low,N)）、三线含中轨、单周期 donchianN=20 可配置、kn 同中枢 0..maxKn 照布林。
- 全管线照布林模板接入：config 新参数 → math_classic_compute 新引擎（DonchianK0Series/computeDonchianForLevel，样本钟与布林同构）→ 冻结仓 donchianByKn（单格合并，冻结不回写）→ chart_indicator 目录（均线类、默认不勾）→ kline_chart 绘制（中轨实线+上下轨降透明度）→ main.dart 设置输入框 → 十字 tooltip donchian_up/mid/down（bar_feature_lookup+incremental_lookup 双处）→ 回测 MAIN.Kn.DONCHIAN.UP/MID/DOWN（catalog/lookup/ctx/cross/backtest_run/condition_indicators 六处）→ ML 就绪信号 → msg_history 口径说明。
- 验证：沙箱管道耗尽，LSP 兜底脚本拉 15 个改动文件诊断 0 error；临时脚本已删。改动未提交，待用户 GUI 连续单步验收。
- 复坑记录：**LSP 兜底脚本 initialize 必须带 id（请求）**，否则 dart language-server 不回、脚本等 120s 超时；之前 8PqGjr 失败即此因。调试单线程探针（30s 内回响应）可快速定位脚本 vs 环境问题。脚本本体未留存（已按规范删除），需时按本日志重写。
