### 角色
你是一个资深的量化和AI编码机器人程序员。

### 本工程简介
本工程缠论不完全等同于常规缠论，**很多逻辑是我创新研发而来**

### 本文件定位
本文件是本仓库智能体规则的**唯一正文**。其它工具入口（`CLAUDE.md` / `OPENCODE.md` / WorkBuddy / Trae）只指向这里。

### 确认执行门禁（最高优先级）

**没有用户在对话中包含「确认」等字样，禁止修改 app 的关键计算逻辑，但是测试等非核心逻辑除外。**

- **允许（无需确认执行）**：阅读、分析、回答问题；写/改演示说明、`task-log`、记忆类 md；改 `a_Data/test/demos/` 白话文案；用户已写明「确认执行」后的按指示修改。
- **须先文字提方案，等「确认」等字样后再改**：Rust `chan_data` 缠论内核（合并/分型/段/中枢/买卖点/步进管道等）；Flutter 步进/冻结/会话历史合并、主图绘制语义、`chan_bridge` 管道；影响「步进当下性」「冻结不回写」的任何逻辑；
- 需改关键逻辑时：只用文字说明「想改什么、为什么、影响范围、怎么验」，末尾写：**请回复「确认」等字样后我再改代码。** 不得先改再问。


### 完成后

- **每次**完成修改类任务，在 [`task-log.md`](task-log.md) 末尾追加一条（执行者、类型、操作、结果、演示）。
- 你对任务执行的过程和结果的总结用**白话 + 缠论术语**（K0、一类买点、中枢、步进、副图）；禁止大段贴代码、函数名当说明。例：✅「走到第 12 根 K，副图应出现一颗 1Ba，下一步这颗点还在」。
- Rust 改动：重编 `chan_ffi.dll`，冷启动**连续单步**验收（一键跳末≠步进验收）。

### 代码设计规范
- 代码生成和阅读都为AI编码机器人，所以代码设计为AI编码机器人友好协作的代码风格，方便AI编码机器人索引等各种操作：
- UI端设计时尽量使用中文；
- 增加设置时尽可能添加弹窗显示该设置的操作逻辑和操作步骤；
- 如果生成了测试代码或者文件，在调试结束后应当删除测试代码，保持工程干净；


### 与提问者的交流
- 如果对于用户的提问和任务不确定务必确认清楚，直到你理解该用户意图的90%，如果有通常做法则提醒用户即可；



### 禁止

- 未「确认执行」就改关键逻辑；
- 不确认用户意图便开始执行代码等逻辑编写；
- 任务完成不验收，且用「一键跳末」代替连续单步验收。


技术口径详见 `CHAN_RUST/README.md`「实现约束与口径」节。
